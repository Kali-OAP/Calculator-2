def celsius():
  while True:
    try:
      valor = int(input("Ingrese el F: "))
      restar = valor - 32
      dividir = restar / 1.8
      redondear = round(dividir)
      print(redondear)
    except ValueError:
      print("Ingrese un valor")
celsius()
